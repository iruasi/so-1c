#ifndef CONSOLAMEMORIA_H_
#define CONSOLAMEMORIA_H_
/*
 * Este archivo no tiene tanto sentido, pero es para no dejar
 * consolaMemoria como una funcion implicita al main.c
 */

#define MAXOPCION 100

void showOpciones(void);
void consolaMemoria(void);

#endif
